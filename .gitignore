# Tooling scratch — logs and session state, not project files
.gstack/
.claude/
.agents/

# Editor/workspace config
*.code-workspace

# Local issue tracker — issues stay out of the public repo
.scratch/

# Supabase CLI scratch (created by `npx supabase …` during backups)
supabase/.temp/
